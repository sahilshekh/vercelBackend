// const Client = require('./Client');
// const User = require('./User');

// // One client can have multiple users
// Client.hasMany(User, { as: 'users' });
// User.belongsTo(Client);

// module.exports = { Client, User };
